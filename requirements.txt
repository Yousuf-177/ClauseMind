# =============================================================================
# Multi-Document RAG Legal Assistant — Python Dependencies
# =============================================================================

# --- Phase 0: Setup ---
google-genai>=1.0.0        # Gemini API SDK (embeddings + generation)
python-dotenv>=1.0.0        # Environment variable management

# --- Phase 1: Document Ingestion (activate when starting Phase 1) ---
# PyMuPDF>=1.24.0            # PDF parsing with page number preservation
# langchain-text-splitters>=0.2.0  # RecursiveCharacterTextSplitter

# --- Phase 2: Embedding & Vector Store (activate when starting Phase 2) ---
# chromadb>=0.5.0            # Vector database
# numpy>=1.26.0              # Numerical operations

# --- Phase 3: Retrieval Pipeline (activate when starting Phase 3) ---
# rank-bm25>=0.2.2           # BM25 sparse retrieval
# transformers>=4.40.0       # HuggingFace for reranker model
# torch>=2.2.0               # PyTorch backend for reranker
# sentence-transformers>=3.0.0  # Embedding utilities

# --- Phase 4: Generation & Guardrails (activate when starting Phase 4) ---
# (uses google-genai from Phase 0)

# --- Phase 5: Evaluation (activate when starting Phase 5) ---
# ragas>=0.1.10              # RAG evaluation framework
# datasets>=2.19.0           # HuggingFace datasets (RAGAS dependency)

# --- Phase 6: API & UI (activate when starting Phase 6) ---
# fastapi>=0.111.0           # API framework
# uvicorn>=0.29.0            # ASGI server
# streamlit>=1.35.0          # Frontend UI
# python-multipart>=0.0.9    # File upload support for FastAPI
